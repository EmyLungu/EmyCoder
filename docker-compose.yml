services:
  api:
    container_name: emycoder-api
    build: .
    ports:
      - "80:80"
    volumes:
      - ./app:/code/app
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp:/tmp
  
  # tunnel:
  #   image: cloudflare/cloudflared:latest
  #   container_name: "emycoder-tunnel"
  #   command: tunnel --url http://emycoder-api:80
