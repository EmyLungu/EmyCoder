[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "EmyCoder"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
